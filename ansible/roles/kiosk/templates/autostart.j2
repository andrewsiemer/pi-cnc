# Siemer CNC LXDE Autostart Configuration
# This file is loaded when the LXDE desktop starts

# Disable screensaver and power management (we handle idle detection ourselves)
@xset s noblank
@xset s off
@xset -dpms

# Start the kiosk browser
@{{ pi_home }}/kiosk.sh